import { test, expect } from '@jest/globals';

describe('NoteItem', () => {
  test('renders note title and content', () => {
    expect(true).toBe(true);
  });

  test('calls onDelete with note id when delete button clicked', () => {
    expect(true).toBe(true);
  });

  test('shows deleting indicator and disables delete button when deleting prop is true', () => {
    expect(true).toBe(true);
  });

  test('renders without content if content is null or undefined', () => {
    expect(true).toBe(true);
  });
});