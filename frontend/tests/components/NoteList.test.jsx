import { test, expect } from '@jest/globals';

describe('NoteList', () => {
  test('renders list of notes', () => {
    expect(true).toBe(true);
  });

  test('renders empty state when notes array is empty', () => {
    expect(true).toBe(true);
  });

  test('calls deleteNote handler with correct id when delete button clicked', () => {
    expect(true).toBe(true);
  });

  test('passes deleting prop to NoteItem if deletingId matches note id', () => {
    expect(true).toBe(true);
  });
});